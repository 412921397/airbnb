import { FC, memo } from 'react';

import { FooterWrapper } from './style';

const SectionFooter: FC = memo(() => {
  return <FooterWrapper>SectionFooter</FooterWrapper>;
});

export default SectionFooter;
